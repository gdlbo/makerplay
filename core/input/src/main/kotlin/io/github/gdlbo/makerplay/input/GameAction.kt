package io.github.gdlbo.makerplay.input

enum class GameAction {
    UP, DOWN, LEFT, RIGHT, OK, CANCEL, SHIFT, MENU, PAGE_UP, PAGE_DOWN,
    ESCAPE, CONTROL, TAB, DEBUG, POINTER_DOWN, POINTER_MOVE, POINTER_UP,
}