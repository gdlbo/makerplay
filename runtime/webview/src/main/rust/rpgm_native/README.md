# `librpgm_native` (Rust)

Android JNI bridge for RPG Maker asset IO / decrypt / index listing.

- Built by Gradle task `cargoNdkRpgmNative` (`cargo ndk`) into `src/main/jniLibs/*/librpgm_native.so`
- Kotlin surface: `io.github.gdlbo.makerplay.runtime.webview.nativebridge.RpgmNative`
